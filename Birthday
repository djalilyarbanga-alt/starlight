<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Joyeux anniversaire 💖</title>
  <style>
    :root{
      --bg1:#0f172a;
      --bg2:#09203f;
      --card:#0b1220cc;
      --accent:#ff7ab6;
      --glass: rgba(255,255,255,0.06);
      --text:#e6eef8;
      --muted:#b6c6da;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      min-height:100vh;
      font-family: "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background: radial-gradient(1200px 600px at 10% 10%, rgba(255,122,182,0.08), transparent 6%),
                  linear-gradient(135deg, var(--bg1), var(--bg2));
      display:flex;
      align-items:center;
      justify-content:center;
      padding:32px;
      color:var(--text);
    }

    .card{
      width:100%;
      max-width:780px;
      border-radius:18px;
      padding:34px;
      background:
        linear-gradient(180deg, rgba(255,255,255,0.03), rgba(255,255,255,0.01)),
        var(--card);
      box-shadow: 0 10px 30px rgba(2,6,23,0.6), inset 0 1px 0 rgba(255,255,255,0.02);
      backdrop-filter: blur(6px) saturate(120%);
      position:relative;
      overflow:hidden;
      border:1px solid rgba(255,255,255,0.04);
    }

    .sparkle {
      position:absolute;
      right:-40px;
      top:-40px;
      width:180px;
      height:180px;
      background: radial-gradient(circle at 30% 30%, rgba(255,255,255,0.14), transparent 10%),
                  radial-gradient(circle at 70% 70%, rgba(255,122,182,0.08), transparent 8%);
      transform: rotate(25deg);
      filter: blur(6px);
      opacity:0.9;
      pointer-events:none;
    }

    h1{
      margin:0 0 10px 0;
      font-size:22px;
      letter-spacing:0.2px;
    }

    .subtitle{
      color:var(--muted);
      margin-bottom:18px;
      font-size:14px;
    }

    .poem{
      line-height:1.6;
      font-size:16px;
      color:var(--text);
      white-space:pre-line;
      margin-bottom:22px;
    }

    .heart-line{
      display:flex;
      align-items:center;
      gap:10px;
      margin-bottom:18px;
    }
    .heart{
      display:inline-grid;
      place-items:center;
      width:54px;
      height:54px;
      border-radius:12px;
      background: linear-gradient(135deg, rgba(255,122,182,0.14), rgba(255,122,182,0.06));
      box-shadow: 0 6px 18px rgba(255,122,182,0.06);
      font-size:26px;
    }

    .controls{
      display:flex;
      gap:10px;
      flex-wrap:wrap;
    }

    button{
      background:linear-gradient(180deg, rgba(255,255,255,0.03), rgba(255,255,255,0.01));
      border:1px solid rgba(255,255,255,0.04);
      color:var(--text);
      padding:10px 14px;
      border-radius:10px;
      cursor:pointer;
      font-size:14px;
      transition:transform .12s ease, box-shadow .12s ease;
    }
    button:active{ transform: translateY(1px); }
    .btn-primary{
      background: linear-gradient(90deg, var(--accent), #ffb3d6);
      color:#04131f;
      font-weight:600;
      box-shadow: 0 8px 20px rgba(255,122,182,0.12);
      border:none;
    }

    .notice{
      margin-top:14px;
      color:var(--muted);
      font-size:13px;
    }

    @media (max-width:520px){
      .card{ padding:20px; border-radius:14px; }
      .heart{ width:46px; height:46px; font-size:22px; }
      h1{ font-size:18px; }
    }
  </style>
</head>
<body>
  <div class="card" role="article" aria-label="Message d'anniversaire">
    <div class="sparkle" aria-hidden="true"></div>

    <div class="heart-line">
      <div class="heart" aria-hidden="true">💖</div>
      <div>
        <h1>Joyeux anniversaire à celle que mon cœur a choisi pour toujours.</h1>
        <div class="subtitle">Un message venu du fond de l'âme — à lire à voix haute si tu veux.</div>
      </div>
    </div>

    <div id="poem" class="poem">
Si la vie était un voyage, tu en serais la plus belle destination.
Chaque jour passé à tes côtés me prouve que l’amour n’est pas qu’un mot,
mais une force qui nous pousse à aller plus loin, à croire, à persévérer.
Peu importe les rêves que nous poursuivons, je sais qu’à deux, rien ne nous résiste —
parce que ton regard m’encourage, et ta foi en moi m’élève.

Merci pour ta présence douce, pour tes prières discrètes mais puissantes,
pour cette lumière que tu poses sur chacun de mes projets.
Tu es ma paix quand tout vacille, mon repère dans la tempête,
la mélodie que je veux entendre chaque matin.

Et même si les mots me trahissent, sache que ton importance dépasse
tout ce que le langage peut contenir.
Tu es ce “pour toujours” que mon âme a reconnu sans hésiter.

Alors en ce jour, je ne te souhaite pas seulement un joyeux anniversaire —
je te souhaite une vie à la hauteur de ton cœur, et des rêves à la mesure de ton amour. 💖✨
    </div>

    <div class="controls" role="group" aria-label="Actions message">
      <button id="copyBtn" title="Copier le texte">📋 Copier le message</button>
      <button id="downloadBtn" title="Télécharger en .txt">⬇️ Télécharger (.txt)</button>
      <button id="voiceBtn" class="btn-primary" title="Lire à voix haute">🔊 Lire à voix haute</button>
    </div>

    <div class="notice" id="notice">Astuce : clique sur "Lire à voix haute" pour entendre le message (navigateur compatible requis).</div>
  </div>

  <script>
    const poemText = document.getElementById('poem').innerText.trim();
    const copyBtn = document.getElementById('copyBtn');
    const downloadBtn = document.getElementById('downloadBtn');
    const voiceBtn = document.getElementById('voiceBtn');
    const notice = document.getElementById('notice');

    copyBtn.addEventListener('click', async () => {
      try {
        await navigator.clipboard.writeText(poemText);
        copyBtn.textContent = '✅ Copié';
        setTimeout(()=> copyBtn.textContent = '📋 Copier le message', 2000);
      } catch(e){
        alert('Impossible de copier automatiquement. Sélectionne le texte et copie manuellement.');
      }
    });

    downloadBtn.addEventListener('click', () => {
      const blob = new Blob([poemText], {type:'text/plain;charset=utf-8'});
      const a = document.createElement('a');
      a.href = URL.createObjectURL(blob);
      a.download = 'message_anniversaire.txt';
      document.body.appendChild(a);
      a.click();
      a.remove();
      setTimeout(()=> URL.revokeObjectURL(a.href), 2000);
    });

    voiceBtn.addEventListener('click', () => {
      if(!('speechSynthesis' in window)){
        notice.textContent = "La synthèse vocale n'est pas supportée par ce navigateur.";
        return;
      }
      const utter = new SpeechSynthesisUtterance(poemText);
      utter.lang = 'fr-FR';
      utter.rate = 0.95;
      utter.pitch = 1.02;
      window.speechSynthesis.cancel();
      window.speechSynthesis.speak(utter);
    });
  </script>
</body>
</html>
